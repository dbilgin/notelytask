server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Revalidate Flutter assets so new releases do not reuse stale app code.
    add_header Cache-Control "no-cache" always;

    location / {
        try_files $uri $uri/ /index.html;
    }
}
